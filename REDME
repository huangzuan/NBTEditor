# NBT 编辑器

一个基于 Java Swing 的 Minecraft NBT 文件编辑器，支持查看、修改、添加和删除节点，方便管理游戏存档和数据包。

## 功能

- 打开 NBT 文件（`.nbt`）
- 查看 NBT 树结构
- 双击叶子节点修改数值
- 添加新的 Compound/List/String 节点
- 删除节点（根节点除外）
- 保存文件
- 快捷键支持：`Ctrl+S`（Windows/Linux）或 `⌘+S`（Mac）保存

## 使用方法

1. 下载并运行 `NBTEdit.jar`
2. 打开你的 NBT 文件
3. 选中节点进行操作：
   - 双击修改节点数值
   - 点击 **添加节点** 添加新节点
   - 点击 **删除节点** 删除选中节点
4. 点击 **保存文件** 或使用快捷键保存修改

## 依赖

- Java 17 或以上
- [Querz NBT](https://github.com/Querz/NBT)（已打包在 jar 内）

## 打包

项目结构建议：
